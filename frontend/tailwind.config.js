/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{jsx,tsx}"],
  theme: {
    extend: {
      fontSize: {
        customsm: "0.8rem", // Change the small text size to 0.75rem
      },
    },
  },
  plugins: [],
};
